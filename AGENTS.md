# 仓库协作约定

Cue 是持久、可观察的本地结构化进程执行内核。本仓库同时维护实现和功能提案。

## 阅读顺序与依据

1. 阅读 [FP-0000](fps/FP-0000-governance.md)，确认提案流程和公开契约变更规则。
2. 阅读改动涉及的 FP；[FP-0001](fps/FP-0001-structured-execution-kernel.md) 定义执行内核。
3. 阅读 [愿景](VISION.md)、[设计原则](PRINCIPLES.md) 和 [架构](ARCHITECTURE.md)。
4. 检查实际代码、测试、CLI 和当前运行结果，确认实现与提案的差异。

流程冲突以 FP-0000 为准。愿景和原则提供方向，不重复定义 FP 的技术契约。提案合入、
功能实现、发布和当前进程使用的版本是不同事实，不能互相推定。

## 写作与标题

- 新增或修改的文档标题使用中文；提案的元数据标题、一级标题和正文使用中文。
  Cue、IPC、API 和代码标识可按需保留，已有英文文档不要求顺带全文翻译。
- FP 标题用中文表达主题，例如“执行级资源管理”；正文首次使用可注明 Execution。
  元数据 title 不含 FP 编号，文件名和一级标题保留 FP-NNNN 的四位编号格式。
- PR 标题使用英文，保留 zendev 的 emoji、type、scope 语法；PR 正文沿用中文模板。
  提案 PR 使用 `propose`、`revise`、`supersede` 表达动作，例如
  `📝 docs(fp): propose execution-level resource management`。提交描述与PR标题要求一致，使用英文。
- 作者填写 GitHub 用户名，不加 `@`。使用 [提案模板](templates/fp.md)，保留所需章节。
- 区分观察到的行为、拟议契约和未来验收，不把提案中的命令写成已经可用的产品说明。

## 提案与实现边界

- 改变公开契约时先提交 FP，具体触发条件以 FP-0000 为准。
- 候选提案直接进入 `fps/`，不建立 drafts 目录，也不引入 Accepted 等生命周期字段。
- 分配编号前检查现有文件和远端提案 PR；索引由工具生成，不手工维护第二套状态。
- 本仓库图关系和术语规则以 `proposal.toml`、schema 和 FP 为准，不照搬其他仓库元数据。
- 提案与实现分别评审。新增原则、指南或 AGENTS.md 不能替代公开契约的提案流程。

## 开发与交付

- 按 [贡献指南](CONTRIBUTING.md) 选择检查，使用仓库固定的工具版本和现有 PR 模板。
- 在独立 worktree 中修改，保留主工作区和其他任务的未提交内容。
- Core 拥有执行语义；Composition 实现物理机制，不能静默改写逻辑 Scope 或计划。
- CLI 变更验证实际二进制、输出和退出码；进程生命周期变更验证取消、退出和故障路径。
- 完成后报告实际修改、校验结果和未验证部分；草稿 PR、CI 通过、合并及发布分别说明。
