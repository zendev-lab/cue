# 设计原则

这些原则帮助评审 Cue 的设计取舍，不独立定义类型、协议或提案状态。精确契约见
[FP-0001](fps/FP-0001-structured-execution-kernel.md)，流程见
[FP-0000](fps/FP-0000-governance.md)。

## 执行语义封闭，实现机制开放

Core 决定计划含义和结果聚合；Composition 选择物理实现。替换 provider 不得增加
计划变体、动态步骤或改变顺序与并行语义。

## 上下文与身份显式

Scope、Execution 和 Step 的身份不能来自环境中的会话游标或隐藏全局状态。逻辑输入与
物理实现上下文分开，环境变化按计划中的规则传递。

## 状态只有明确的所有者

每个执行状态、进程、PTY、输出和资源占用都有生命周期负责人。前端断开不等于任务
结束，观察者不接管执行状态。新增持久状态必须解释它与已有事实源的关系。

## 事实提交与物理执行分离

归约器决定语义状态，运行时落实物理行为。幂等重放不能重复启动进程；执行终态不能替代
物理静止证据。未知执行归属必须显式暴露，不能猜测成功或安全重试。

## 组合可以检查

依赖、端口贡献与顺序应在启动时校验。缺失 provider、冲突和不兼容假设应产生可诊断
错误，不依赖导入顺序、隐式覆盖或运行时临时查找服务。

## 用户边界上的证据优先

测试覆盖用户能观察的行为和失败路径。CLI 需要真实命令与退出码证据，进程能力需要
生命周期证据，打包能力需要安装产物证据；注册成功或单元测试不能证明端到端可用。

## 设计、实现与发布分别记录

FP 保存设计决定，不存储功能完成状态。代码是当前实现证据，不能自动改写设计；文档
合入也不能证明二进制、部署或运行中的 daemon 已更新。
