# 项目愿景

Cue 为人类开发者和智能体提供持久、可观察的本地结构化执行能力。一次任务拥有明确的
执行身份，客户端断开后仍可查询、取消和重新连接其终端，不依赖某个前端一直在线。

## 期望结果

开发者能从执行记录理解哪个步骤运行、产生什么输出、为何失败，以及进程是否真正退出。
集成方能通过显式类型和幂等提交复用这套能力，而不必自行维护进程、PTY 和恢复状态。

任务组合保持有限、静态和结构化。顺序、并行、环境传递和取消都有可解释的含义；扩展
执行环境时不改变同一个计划的逻辑意义。

## 架构方向

保留小而封闭的执行内核，通过可检查的 Composition 绑定存储、进程启动、工作目录与
其他物理机制。每份可变状态都有明确的所有者，创建、共享、终止和失败恢复能被验证。

会话、触发、重试、审批和资源策略不成为 ExecutionPlan 的隐式输入。需要由 Cue 提供的
上层能力，应先通过 FP 说明边界、协议和生命周期，再连接到内核。
[执行级资源管理提案](fps/FP-0002-execution-resource-management.md) 是这一方向的设计记录，
不代表当前版本已经提供资源管理。

## 文档边界

本文描述方向，不定义新的技术契约或排期。精确语义由 [功能提案](fps/README.md) 记录，
当前能力由实现和运行证据确认；[设计原则](PRINCIPLES.md) 用于讨论取舍。
[SPARK.md](SPARK.md) 保留项目起源和演进背景。
