[package]
name = "cue-tui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "TUI frontend for Cue: mode switching, command input, job display"
repository.workspace = true
license.workspace = true
autobins = false

[[bin]]
name = "cue-tui"
path = "src/main.rs"

[dependencies]
anyhow.workspace = true
crossterm.workspace = true
cue-client = { path = "../cue-client" }
cue-core = { path = "../cue-core" }
cue-language = { path = "../cue-language" }
cue-protocol = { path = "../cue-protocol" }
ratatui.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true

[dev-dependencies]
cue-daemon = { path = "../cue-daemon" }
