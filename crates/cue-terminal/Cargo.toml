[package]
name = "cue-terminal"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Ghostty-backed foreground terminal model for cue-shell"
repository.workspace = true
license.workspace = true

[dependencies]
crossterm.workspace = true
libghostty-vt.workspace = true
# Pin the sys crate independently: libghostty-vt's dependency is a caret range,
# while our verified source/toolchain closure is specific to sys 0.2.1.
libghostty-vt-sys.workspace = true
ratatui.workspace = true
thiserror.workspace = true

[dev-dependencies]
ratatui.workspace = true
